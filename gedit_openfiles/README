Getting the file browser location

- new version
bus = window.get_message_bus()
print bus.send_sync('/plugins/filebrowser', 'get_root').uri

- older
bus = window.get_message_bus()
print bus.send_sync('/plugins/filebrowser', 'get_root').get_value('uri')

- Notification of url change.
bus.connect('/plugins/filebrowser', 'root_changed', callback)
def callback(bus, msg):
    pass
